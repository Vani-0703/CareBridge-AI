 "use client"; import {useState} from "react"; export default function Assistant(){const[q,setQ]=useState("");const[a,setA]=useState("");return <div className="page"><span className="badge">Assistant</span><h1>Ask CareBridge</h1><div className="card"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Ask a healthcare information question"/><button className="btn" style={{marginTop:12}} onClick={()=>setA(q?`Demo response: I can organize this question, but I will not invent medical facts. Verify health information with a qualified professional.`:"Enter a question first.")}>Send</button>{a&&<p className="muted">{a}</p>}</div></div>}