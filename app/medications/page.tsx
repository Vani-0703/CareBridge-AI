 "use client"; import {useState} from "react"; export default function Medications(){const[q,setQ]=useState("");const[s,setS]=useState(false);return <div className="page"><span className="badge">Medication information</span><h1>Medication lookup</h1><div className="card"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Medication name"/><button className="btn" style={{marginTop:12}} onClick={()=>setS(true)}>Search</button>{s&&<p className="muted">Production version should use a verified medication database. This demo does not invent drug facts.</p>}</div></div>}